entryPoints:
  web:
    address: ":80"

api:
  dashboard: true
  insecure: true

providers:
  docker:
    exposedByDefault: false
    network: "traefik-net"
  file:
    directory: "/etc/traefik/dynamic"
    watch: true